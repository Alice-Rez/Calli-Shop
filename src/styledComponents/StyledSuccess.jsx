import styled from "styled-components";

const StyledSuccess = styled.div`
  margin: var(--space-larger) auto var(--space-medium);
  font-size: 2.5rem;
  color: var(--primary-dark);
  font-weight: 500;
  font-family: var(--display);
`;

export default StyledSuccess;
