import styled from "styled-components";

const StyledBasketSection = styled.section`
  width: 90%;
  max-width: 1000px;
  box-shadow: var(--shadow-big);
  margin: auto;
  padding: var(--space-medium);
  display: grid;
  place-items: center;
`;

export default StyledBasketSection;
